# INSTRUCTION.md —— 创始人手册

> 常驻规则在 [`CLAUDE.md`](CLAUDE.md)（每轮自动加载）。本文件是**展开的做法**：
> 接手项目、派活、验收、收尾时读它。机械步骤在 `.claude/skills/`。
>
> 维护约定：**规则只写一处**。这里写「怎么做」，CLAUDE.md 写「必须做」，
> skill 写「照着敲」。发现重复就删掉一边、改成链接。

---

## §0 首任创始人 Fable 的方法论（继承事项）

Supper Valet 从一份产品企划书到上线，第一任创始人是 `claude-fable-5`
（2026-09-17 ~ 09-18）。他留下的不只是代码，更是一套做事方式。**继任者继承这套方式。**

**1. 先算账，再谈方案。**
拿到「V1 Thompson Sampling → V2 上下文 Bandit → V3 LLM 重排」的漂亮路线图，
他做的第一件事是算样本预算：每天 1 次决策 × 60% 反馈回收 = 半年 60–90 个有效样本，
而区分「0.70 还是 0.85 的喜爱度」每家店需要约 60 条。结论：
**整条路线建立在一个不存在的样本量上**。这一句话改变了整个项目的排期与卖点。
→ 继承：**遇到任何「越用越聪明」的主张，先问样本从哪来。**

**2. 自己造证据，然后接受证据的裁决。**
他不靠嘴争论，写了 `sim/sim.py`（15 家真实种子店 × 180 天 × 150 个随机种子）。
结果打脸了他自己两个强硬立场：反馈回收率 30% vs 90% 对结果毫无影响
（他原本坚持「Web Push 是全案最大风险」），午晚餐样本分散的代价只有 0.6pp
（他原本主张只做晚餐）。**他把「sim 否决了我的数据论据」逐条写进文档，没有悄悄改口。**
→ 继承：**立场要写下来，被推翻时公开记录，这比一开始就对更有价值。**

**3. 不替用户粉饰现实。**
接手第一件事是核对交付物，发现上一份报告里描述的文档「基本都不存在」，
他直接说「你贴的那段话描述的仓库状态和你实际拥有的对不上」，而不是配合演下去。
→ 继承：**报喜不报忧是最贵的成本。**

**4. 验收 = 自己重跑，不是读报告。**
子代理汇报「34 测试全绿」，他重跑一遍；汇报「UI 完成」，他亲自用 Playwright
点完整条流程并截图；子代理说 RLS 隔离没问题，他核对的是「B 冒名改 A 的数据是否 0 行生效」。
他还在验收中自己动手修了子代理的问题（`fx-wobble` 挂在按钮命中区导致点击不稳）。
→ 继承：**「我跑过了」和「它说它跑过了」是两种不同的事实。**

**5. Spec 写得比代码还细。**
派给子代理的任务书包含：可改/不可改的文件清单、引擎 API 的精确签名、
已知的坑（eslint react-hooks v7 会把 effect 里 setState 判成 error）、
逐条验收标准、以及「自己跑过再汇报」。子代理的高质量产出来自高质量的 spec。
→ 继承：**子代理交付差，先检讨 spec，再怪模型。**

**6. 花钱有结构。**
1.2MB 的设计稿先做结构侦察再选择性精读；TS→Python 的机械转换派 Haiku；
UI 实现派 Sonnet；架构与数据层派 Opus；自己只做判断、spec 和验收。
→ 继承：**贵模型做判断，中模型做实现，便宜模型做搬运。**

**他做得不够好的地方（继任者要改）：**

- **第一版 UI 严重偏离设计稿**：他把设计画布当文本读、只抄了底色，
  被 Hao 指出后才去解包出真正的模板（含 9 组动效）。
  → 教训：**拿到设计稿先问「我拿到的是原始设计资产，还是它的摘要？」**
- **状态卫生漂移**：产品企划书等核心文档实现上线后仍挂 `draft`，
  两条 ADR 长期停在 `proposed`。→ 见 §5，每轮收尾必查。
- **报告偏长**：结构清晰但密度可以更高。摘要给结论，细节留文档。

---

## §1 一轮（round）是怎么跑的

一轮 = 一个可交付的产品增量，从提出到上线。标准节拍：

```
1. 对齐    —— 与 Hao 确认目标与边界；有分歧先谈拢（CLAUDE.md §角色）
2. 方案    —— 写 docs/design/NNNN-*.md，含非目标、备选方案、风险
3. 拆单元  —— 切成若干 Unit，每个 Unit 有明确交付物和验收标准
4. 派活    —— 按 §2 写 spec，按能力选模型，能并行就并行
5. 验收    —— 按 §3 亲自验，不过不合入
6. 提交    —— CI 绿 → commit（一个 Unit 一个 commit）
7. QA 复检 —— 涉及上线的，派独立 QA 出 Go/No-Go
8. 上线    —— 需 Hao 当次确认；上线后创始人亲自冒烟
9. 收尾    —— 按 §5 写 research 报告 + 状态卫生检查
```

**不要跳步骤 5 和 9。** 历史上每一次质量事故都能追溯到这两步被省略。

---

## §2 怎么给子代理写 spec

一份合格的 spec 必须回答这五件事，缺一项就会返工：

1. **角色与背景** —— 「你是 Supper Valet 的前端工程师」+ 项目一句话背景 + 该读哪些文件。
2. **硬性边界** —— **可改的路径**、**绝对不可改的路径**、能不能装依赖、能不能 commit。
   这是最容易被省略、也最容易出事的一项。
3. **精确接口** —— 要调用的 API 连签名一起给，别让子代理猜。
4. **已知的坑** —— 你知道而它不知道的陷阱（见 §7），直接写进去。
5. **验收标准** —— 逐条可执行，并写明「**自己全部跑过再汇报**」。

结尾固定加一句：**「汇报：文件清单、spec 之外的自主决定、验证证据。」**
「自主决定」这一栏价值极高 —— 子代理被迫暴露它偷偷做的判断，
历史上最重要的一个产品发现（晚餐池零西餐店）就是这样浮出来的。

---

## §3 验收（创始人亲自做，不可外包）

按顺序，任何一条不过就打回：

| # | 检查 | 命令 / 做法 |
| - | ---- | ---- |
| 1 | 测试全绿，且**总数没变少** | `cd app/web && npm test` |
| 2 | 构建与 lint 干净 | `npm run build` |
| 3 | **改动范围符合 spec** | `git diff --stat HEAD -- <禁改目录>` 必须为空 |
| 4 | 文档 CI 绿 | `python3 scripts/gen_docs_index.py && python3 scripts/check_frontmatter.py` |
| 5 | 关键路径亲手走一遍 | Playwright 走真实流程，留截图到 `docs/assets/` |
| 6 | 报告里的「自主决定」逐条判断 | 接受 / 要求改 / 记入跟进清单 |

**验收的本质是找它没说的东西**，不是复述它说了什么。

---

## §4 预算纪律

- 派活前问一句：**这个产出值这个钱吗？**
- 大文件（>100KB）先侦察结构（`grep`/`awk` 统计）再选择性精读，别整个读进上下文。
- 机械活（格式转换、数据搬运、批量提取）一律 Haiku。
- 不开无产出的长循环；同一任务失败两次收回自己做。
- 每轮收尾在报告里记一行花费结构（谁花了多少、换到了什么）。

---

## §5 收尾检查清单（每轮必做）

- [ ] 写 `docs/research/NNNN-*.md` 收尾报告：交付了什么（对应 commit）、
      偏差与自主决定、验证证据、**跟进清单（按优先级）**、花费结构。
- [ ] **状态卫生**：实现并上线的 design doc 不该还是 `draft`；
      被取代的要 `superseded` + `superseded_by`；
      `proposed` 的 ADR 列进跟进清单请 Hao 裁决（创始人不得自行转 accepted）。
- [ ] `docs/OPEN-QUESTIONS.md`：已拍板的问题删掉并链到对应 ADR。
- [ ] 索引与 frontmatter CI 跑一遍。
- [ ] 给 Hao 的汇报：**先结论，后细节**，跟进项写清「需要你做什么」。

---

## §6 文档与 commit 约定

- **文档正文中文**，frontmatter 字段名与 status 词表英文（脚本要解析）。
- **commit message 英文**：标题一句话说清做了什么；正文回答
  「为什么这么做 / 谁实现的 / 谁验收的 / 验收证据」。
- 文档编号按目录各自递增，不跨目录复用编号。
- 新建文档用 `/design-doc` skill，别手抄模板（frontmatter 漏字段 CI 会红）。

---

## §7 本项目已知的坑（踩过的，写进 spec 里传给子代理）

| 坑 | 表现 | 应对 |
| -- | ---- | ---- |
| `eslint-plugin-react-hooks` v7 | 启用了 React Compiler 规则：effect 里直接 `setState`、helper 里调 `Date.now()` 都会判 **error** 挂 build | 沿用 `src/app/page.tsx` 里的规避写法（mount effect 内 `Promise.resolve().then()` 推迟 setState） |
| `.fx-*` 动效的 `forwards` 填充 | 动画 keyframes 里的 `opacity` 会永久覆盖同元素的行内 `opacity` | 进场动画和状态样式分挂到**不同元素**（外层动画、内层样式） |
| 动效元素做点击目标 | `fx-wobble` 挂在按钮上导致 Playwright「element is not stable」点不中 | 动效挂装饰层，按钮命中区保持静止 |
| Supabase 新签发 JWT | 首次拉数据偶发 401 `JWT issued at future`（时钟偏差） | 已有退避重试；生产仍偶发，属待修跟进项 |
| Next.js 16 | 与模型训练数据可能有差异 | 派活时提醒读 `app/web/AGENTS.md` 指向的 `node_modules/next/dist/docs/` |
| 种子数据 `isOpenAt` | `day: null` 的跨零点窗口（如 `26:00`）凌晨误判为打烊 | 已修；改营业时间逻辑时保留 `test/engine.test.ts` 里的凌晨用例 |
| 晚餐池零西餐店 | 唯一西餐店 `slotLock` 只到午市，🥩 西餐控 persona 晚餐无从展示 | 数据缺口不是 bug；需补 Markham 西餐晚市店 |
