# app/

产品代码。目前是空的 —— **技术选型尚未决定**。

选型会以一篇 ADR 落在 `docs/decisions/`，理由和代价都写清楚，然后代码才进来。
在那之前这里保持空白是刻意的：不想让一个随手 `create-next-app` 的结果
变成事实上的架构决策。

下一步：Fable 评审 design doc → 输出选型建议 → 写 ADR-0002 → 脚手架落地。
